import React from 'react';
import {Text, View} from 'react-native';

const ControlledWebSocket = () => {
  return (
    <View>
      <Text>ControlledWebSocket</Text>
    </View>
  );
};

export default ControlledWebSocket;
